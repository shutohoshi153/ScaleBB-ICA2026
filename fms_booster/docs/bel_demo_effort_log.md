**日本語** | [English](bel_demo_effort_log.en.md)

# BELデモ 導入負担実測記録(論文§9.2 エビデンス)

作業指示書 `BEL_Demo_WorkInstruction_FMS_20260710.md` §5.5 の規定に基づく工数記録。
**2026-07-24 のフォールバック判断点を経過したため、FMS 統合は実施せず簡易 Python 版
(`scripts/bel_demo/`)を主計算とした。** 従って本記録は「FMS 統合の実測値」ではなく
「簡易 Python 版の実測値」である。論文 §9.2 では FMS 統合実測値は引用できない
(§9.2 は 5 論拠+段階的導入パスのみで構成する)。

**2026-07-29 方針変更(確定):** 簡易 Python の処理時間では実務レベルの説得力に
欠けるため、ドラフト版(7/31)では処理時間に一切言及しない(§7.2・§8.2・§8.4・
§9.2 論拠 4 から数値・実測への言及を除去済み。論拠 4 は定性的記述のみ)。
最終版では、実務導入実績のある FMS Booster での Run 実測値を計測のうえ §9.2 論拠 4 に
記載する。下記「FMS Booster 実行所要時間の計測」参照。FMS 統合工数(改修行数・人日)は
未実施のため引き続き不引用。

## 実測値(2026-07-28、Claude Code 使用)

| 項目 | 実測 |
|---|---|
| FMS 改修モジュール数 / 改修行数 | 0 / 0(未実施。`Base_Model_v251208/` は読取参照のみ) |
| Python 実装 | 新規 5 スクリプト(ESR 割引カーブ再現・率生成・V1a 検証・BEL 主計算・集計作図)+ README |
| 作業工数 | Claude Code セッション 2 回(初版 約 0.5 人時 + ESR 実装仕様準拠化 約 0.5 人時) |
| 率生成の計算時間 | 0.2 秒(6 シナリオ × 3 疾病 × 2 性別 + 全死因、fit 8 回 + project 26 回) |
| ESR 割引カーブ再現 | 1 秒未満(Smith-Wilson 適合 + α 探索) |
| BEL 計算の計算時間 | 0.03 秒(8 MP × 6 シナリオ) |
| V1a 全件突合 | 0.5 秒(6,840 件) |

ESR 実装仕様準拠化(2 回目セッション)の内訳: 金融庁公表資料の取得・読解(イールド
カーブツール xlsx・1柱告示 167 頁からの係数特定)、Smith-Wilson 補外の実装、
ESR_M シナリオ追加、論文 §8 執筆。

追加調査・実装(同セッション続き): 国内解説ソース調査(金融庁 検討状況/暫定決定/
残論点・Q&A、野村資本市場研究所 2022、生命保険論集 上野 2023、Web共済と保険
森本 2025)ののち、告示から生保サブリスク統合(第81条 相関行列)・解約失効
(第61〜63条)・長寿(第57条)・MOCE(第29・30条 資本コスト率3%)を特定し、
`calc_esr_life_risk.py` として実装(計算時間 1 秒未満)。論文 §8.5 追記。

## FMS Booster 実行所要時間の計測(最終版 §9.2 論拠 4 用、最終版までに実施)

**準備完了(2026-08-05):** 実行ケース・入力テーブル・突合スクリプトを整備済み。
実行ケース定義書 `FMS_BoosterRunCases_Tier1_20260805.md` に従い、
`Scenario_Model_BELDemo/`(作成済み・原本と byte 同一)+ `fms_input/` の SQLite 4 本で
RUN-00(疎通)→ RUN-A1(BASE 8MP)→ RUN-B1〜B5(残 5 シナリオ)を実行して計測する。
方式はデータ注入方式(**FMS 改修 0 モジュール・0 行** — シナリオ軸を PROD_CD に折込み、
世代対角線率を TBL_RSKRT へ事前焼込み)。この 0/0 自体が §9.2 導入負担の実測値になる。

| 計測項目 | 実測(記入欄) |
|---|---|
| FMS 1 シナリオ実行所要時間(8 MP、RUN-A1) | (未計測) |
| FMS 6 シナリオ合計(RUN-A1〜B5。参考: RUN-C1 一括) | (未計測) |
| 実行環境(マシンスペック・FMS バージョン・コア数) | (未記入) |
| 計測日 | (未記入) |
| FMS 改修モジュール数 / 改修行数 | 0 / 0(データ注入方式。`diff -rq` で確認) |
| FMS 入力テーブル生成+独立検証(Python 側) | 生成 0.5 秒・V1a-F 検証 23,314 件 合格(2026-08-05) |

計測後、最終版の §9.2 論拠 4 に FMS 実測値を記載する(ドラフト版には処理時間の記載
なし・プレースホルダなし)。なお同じ Run から現価が得られるため、V1b 突合
(全 8 MP ±1%、作業指示書 §7)も同時に実施できる(論文 §10.4 の今後の検証)。

## 論文で利用可能な示唆

- シナリオ生成(Phase 2 再投影)は L の差し替えのみで、1 シナリオあたり秒未満。
  「計算負荷が軽い」(§9.2 論拠 4)の裏付けとして利用可能
- FMS 統合を経ずとも、既存の率パネルとアルゴリズムコアだけで感応度一表が得られた事実は
  「上流配置・同形式出力」(§9.2 論拠 1)の傍証になる
- FMS 上での再実行(V1b)は今後の検証として §10 に記載する
