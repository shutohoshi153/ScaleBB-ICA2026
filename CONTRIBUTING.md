# 共著者・査読者向け 変更手順

このリポジトリは、著者の作業リポジトリと双方向で同期されます。
**共著者は `main` へ直接コミット・push して構いません。** 内容は著者が
作業リポジトリへ取り込む際に確認します。

## 変更の入れ方

### 直接コミット（共著者）

```bash
git pull            # ← 必ず先に実行してください
# 編集
git commit -am "§3.2 の記号定義を SOA (2012) に合わせる"
git push
```

**push の前に必ず `git pull` してください。** 著者が同期した内容を取りこぼすと、
以降の履歴が分岐して解決の手間が増えます。

コミットメッセージには、どの節をどう直したかを一行で書いていただけると、
著者側の確認が早くなります。

### 指摘・相談（査読者、および議論したい場合）

修正案が固まっていない指摘や、方針の相談は Issue でお願いします。
該当箇所は `sections/03_data_and_methodology.md` の §3.1.2 のように、
ファイル名と節番号で示していただけると助かります。

構成に関わる大きな変更（節の追加・削除、章立ての変更）は、
直接コミットする前に Issue で一言ご相談ください。

## 同期の仕組み（知っておくと安心な範囲で）

`main` の内容は、著者の作業リポジトリにある原稿ディレクトリと 1 対 1 で対応しています。
著者側は次の 2 方向で同期しています。

- **公開 → 作業リポジトリ**: 共著者のコミットを差分として取り込み、内容を確認してコミットする
- **作業リポジトリ → 公開**: 著者が加筆した内容を `main` へ反映する

未取り込みの変更が `main` にある状態では、著者側の反映ツールが自動的に停止します。
そのため、共著者のコミットが気づかれずに巻き戻ることはありません。

なお `LICENSE` は公開リポジトリ側だけで管理しており、同期の対象外です。

## 再現パッケージについて

`reproduction/` 配下の 2 パッケージは単体で実行できます。手順と期待される数値は
各 `README.md` を参照してください。数値が合わない場合は、実行環境（Python と
主要ライブラリのバージョン）を添えて Issue にお願いします。

再現実行で生成される `reproduction/backtest/output/` は追跡対象外です。
生成物をコミットする必要はありません。

## データの取り扱い

同梱データには第三者提供の統計が含まれます。再利用の際は
[`DATA_SOURCES.md`](DATA_SOURCES.md) の出典表記と利用条件に従ってください。
リポジトリの `LICENSE`（本文 CC BY 4.0 / コード MIT）はこれらのデータには及びません。
