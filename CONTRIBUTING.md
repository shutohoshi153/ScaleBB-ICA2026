# 共著者・査読者向け 変更手順

このリポジトリは、著者の作業リポジトリから**一方向で同期される公開ミラー**です。
そのため、変更の入れ方に一つだけ重要な制約があります。

## ⚠️ `main` へ直接コミットしないでください

`main` への直接コミットは、**次回の同期で予告なく巻き戻されます**。
同期は作業リポジトリの内容で公開リポジトリを丸ごと置き換える方式のためです。

変更は必ず以下のいずれかの形でお願いします。

## 変更の入れ方

### 1. 指摘・議論 — Issue（推奨）

原稿への指摘、数値の確認依頼、再現手順で詰まった点などは Issue を立ててください。
該当箇所は `sections/03_data_and_methodology.md` の §3.1.2 のように、
ファイル名と節番号で示していただけると助かります。

### 2. 具体的な修正案 — Pull Request

文面の修正案が明確な場合は、`main` 以外のブランチを作って Pull Request を出してください。

```bash
git switch -c fix/section3-notation
# 編集
git commit -am "§3.2 の記号定義を SOA (2012) に合わせる"
git push -u origin fix/section3-notation
```

ブランチ上のコミットは同期の影響を受けません。安心して作業できます。

PR は著者が確認し、**作業リポジトリ側に取り込んだうえで再同期する**形でマージされます。
そのため、PR が「Merged」ではなく「Closed」になることがありますが、
内容が反映されていないという意味ではありません。反映後の内容は `main` でご確認ください。

## 再現パッケージについて

`reproduction/` 配下の 2 パッケージは単体で実行できます。手順と期待される数値は
各 `README.md` を参照してください。数値が合わない場合は、実行環境（Python と
主要ライブラリのバージョン）を添えて Issue にお願いします。

## データの取り扱い

同梱データには第三者提供の統計が含まれます。再利用の際は
[`DATA_SOURCES.md`](DATA_SOURCES.md) の出典表記と利用条件に従ってください。
リポジトリの `LICENSE`（本文 CC BY 4.0 / コード MIT）はこれらのデータには及びません。
